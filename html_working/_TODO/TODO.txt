sauvegarde des utilisateurs sous firefox
  semble ne pas supporter correctement fetch

implémentation des fonctions de réglages dans la db
  	- vérifier la taille minimale que peut donner la caméra

Interface
---------
  bug dans homepage - les T°et Hum sont inversées
    int <> ext
  ajouter la config du nichoir
	  - Triple table -> Capteurs (ID, NAME) - CapteursParameters (ID, ID Capteurs, PIN, parameter) - Capteurslabel (IDcaptParam, ID, Langue, label)
        préremplir la table avec les capteurs et les paramètres en cas d'update, et
        copier la nouvelle bibliothèques - Ecraser le fichier python existant (indication de la version en commentaire),
        autre fichier pour bibliothèques perso
        Bibliothèque par capteur
        Héritage d'une bibliothèque mère qui appelle tous les capteurs à mesurer
         -> nom du capteur.parametre1(2, ...) issu de la DB.

    - info sur l'utilisation
    - comme utilisateur : plus pour ajouter un capteur, moins pour enlever

    sauvegarde dans table sensors
    - table à modifier pour ajouter l'ID
  adapter le display des valeurs
    - choix des capteurs
    - homepage : adapter le display en fonction des capteurs présents

Python
------
  modifier la lecture des capteurs
    - classe dynamique en fonction des capteurs
    - détection in/out

Bash
----
  - adapter le fichier instal
  - Modifier la db


AMELIORATIONS
-------------
Fonction de Tag / commentaires / journal
  stockage des infos dans les fichiers jpeg
  vérification à l'ouverture pour activer un flag
  état d'une icône par fichier en fonction du flag (true/false)
  fonction d'ajout / de suppression
  fonction de publication sur le serveur

Gestion dynamique des capteurs
  installation dynamique des bibliothèques des capteurs

DB
  simplification des tables capteurs
